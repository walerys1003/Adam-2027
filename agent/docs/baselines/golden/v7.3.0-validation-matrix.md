# v7.3.0 Golden Baseline Validation Matrix

Known-good provider behavior captured from real PSTN calls on the dev integration box
before tagging `v7.3.0` (per-agent voice release). Each row has a real passing call with
evidence (call ID + archived engine log capture). Use this as the regression floor.

## Run 1 — 2026-07-02 (feat/agent-voice, full branch)
- **Branch / SHA:** `feat/agent-voice` @ `83de0764` (includes v7.2.1)
- **Validated:** 2026-07-02 (7 live calls: one per provider baseline + one per-agent voice
  override call). All 8 configured provider instances `ready`; `call_history.db` gained
  `voice`/`voice_source` columns additively on a 1,667-record production DB with zero errors;
  agents.db required no migration (voice column existed since v7.0.0, all agents NULL →
  behavior identical to pre-upgrade until a voice is chosen).
- **Transport:** AudioSocket, telephony µ-law 8k.
- **Evidence:** `archived/logs/2026-07-02_v730-voice-validation/` (gitignored — full engine
  capture + per-call slices + RCA).

| # | Provider | Agent/Context | Call ID | Duration | Turns | avg turn ms | Voice / source | Outcome | Status |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Grok (grok3) | demo_grok | `1783042674.496` | 40.1s | 3 | 1102 | — (unmatched context → provider defaults) | completed | PASS |
| 2 | Deepgram Voice Agent | demo_deepgram | `1783042727.500` | 96.1s | 8 | 930 | provider-default | completed | PASS¹ |
| 3 | Google Live | demo_google_live | `1783042835.504` | 71.4s | 2 | 430 | provider-default | completed | PASS |
| 4 | OpenAI Realtime | demo_openai | `1783042920.508` | 96.0s | 6 | 1691 | provider-default (alloy) | completed | PASS |
| 5 | ElevenLabs Agent | demo_elevenlabs | `1783043027.512` | 74.3s | 4 | n/a² | provider-default (platform-managed) | completed | PASS |
| 6 | Local Hybrid Pipeline | demo_hybrid | `1783043108.517` | 97.3s | 4 | 2928 | — (pipelines: voice out of 7.3.0 scope) | completed | PASS³ |
| 7 | **OpenAI Realtime + agent voice override** | demo_openai (voice=marin) | `1783043785.522` | 58.1s | 4 | 867 | **marin / agent** | completed | **PASS** |

**Per-agent voice feature validation (row 7):** voice set to `marin` in the Agents UI (no
engine reload required — agents.db is read per call); engine logged
`Session voice resolved voice=marin source=agent` then
`Using per-call OpenAI voice override voice=marin`; caller confirmed marin audio (clearly
distinct from row 4's alloy); Call History recorded `voice=marin, voice_source=agent` and the
detail modal renders "Voice: marin (from agent)". Rows 2–5 confirm the fallback path
(`provider-default` recorded, provider-configured voice used, no behavior change vs 7.2.x).

Run-1 regression checks: greeting completed on all 7 (transcript-verified); no new ERROR spam
(one benign ARI 404 on post-hangup variable set during call 6 — known teardown race); no
`ProviderDisconnected`/mid-call reconnects; `GET /api/config/providers/meta` 200; Agents-form
voice controls verified in-browser for static (OpenAI), freeform (Grok), platform-managed
(ElevenLabs), and pipeline states.

¹ Deepgram think-model intermittently speaks farewells + the literal "(hangup_call)" before
  issuing the real FunctionCall (2 pre-upgrade occurrences; engine-side handling correct).
  Pre-existing prompt-adherence issue, tracked separately — not a 7.3.0 regression.
² ElevenLabs turn latency is platform-side; engine-measured value not comparable.
³ Local Hybrid's LLM endpoint on the dev box was mid-experiment (non-standard local LLM
  config); its hangup tool-call miss is config drift, tracked separately — 60 successful
  hangup_call executions on record under the standard config.

## Status Legend
- `PASS`: validated with call evidence
- `N/A`: intentionally not claimed for this release
